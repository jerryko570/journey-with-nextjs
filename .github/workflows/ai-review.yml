      - name: Comment on PR
        uses: actions/github-script@v6
        with:
          script: |
            const body = `
## 🤖 AI Code Review Summary

<details><summary>📄 Click to expand result</summary>

${process.env.REVIEW}

</details>

---

### 📌 Risk Evaluation
| Level | Meaning |
|---|---|
| 🟢 LOW | 안정적, 큰 문제 없음 |
| ⚠️ MEDIUM | 개선 및 검토 권장 |
| 🚨 HIGH | 잠재적 위험 존재 |

> AI가 코드 변경사항을 분석하여 자동 리뷰했습니다.

_This review was generated by HuggingFace AI Workflow._
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
        env:
          REVIEW: ${{ steps.review.outputs.text }}
