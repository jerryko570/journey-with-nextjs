- name: Comment on PR
  uses: actions/github-script@v6
  with:
    script: |
      const review = process.env.REVIEW;
      const body = `
## 🤖 AI Code Review Summary  
> Pull Request 자동 분석 결과 입니다.

---

### 🔍 리뷰 결과 요약
<details><summary>📄 Click to expand</summary>

${review}

</details>

---

### 📌 Risk Level 평가
| 수준 | 의미 |
|---|---|
| 🟢 Low | 변경 안전, 큰 문제 없음 |
| ⚠️ Medium | 개선 필요 요소 존재 |
| 🚨 High | 버그/보안/구조적 문제 가능성 |

> AI 분석 기반으로 본 PR의 위험도 결과는 workflow 아래 단계에서 Label로 표시됩니다.

---

### 🧭 참고하면 좋은 개선 방향
- 코드 가독성 / 성능 / 구조적 개선 제안 포함
- 필요 시 추가 테스트 추천
- 모델 분석 기반이므로 최종 판단은 리뷰어와 함께

---

_This review was auto-generated by **HuggingFace AI + GitHub Actions**._  
`;

      await github.rest.issues.createComment({
        issue_number: context.issue.number,
        owner: context.repo.owner,
        repo: context.repo.repo,
        body: body
      });
  env:
    REVIEW: ${{ steps.review.outputs.text }}