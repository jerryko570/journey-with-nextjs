name: AI PR Review (HuggingFace)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run HuggingFace Code Review
        uses: huggingface-actions/code-review@main
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          model: "bigcode/starcoder2-7b"
          language: "javascript,typescript,css,html"

      - name: Comment summary on PR
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: `
🧠 **AI 코드 리뷰 완료!**

✔ 자동 분석이 성공적으로 수행되었습니다.  
🔍 세부 결과는 **Checks → Run HuggingFace Code Review**에서 확인하세요.  

💡 포함 내용  
- 개선 포인트 요약  
- 잠재 오류/리스크 분석  
- 리팩토링 제안  

추후 원하면 코드 자동 수정 PR도 생성해드릴게요 🙂
`
            })
