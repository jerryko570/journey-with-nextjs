name: AI PR Review (HuggingFace)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run HuggingFace Code Review
        uses: huggingface-actions/code-review@main
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          model: "bigcode/starcoder2-7b"
          language: "javascript,typescript,css,html"

      - name: Comment summary on PR
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: `
### 🤖 AI Code Review 완료!

📌 자동 분석 결과가 생성되었습니다.  
🔎 **Checks 탭 → Run HuggingFace Code Review** 에서 세부 내용을 확인할 수 있어요.

---

💡 포함된 내용  
- 코드 개선 제안
- 잠재 버그 포인트
- 리팩토링 추천
- 위험도 분석 (Low/Medium/High)

추가로 자동 리팩토링 PR 기능도 원하면 넣어드릴게요 :)
`
            })
